# Core API framework
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6

# Audio processing
numpy>=1.22.0
soundfile>=0.12.1          # Fast WAV/FLAC/OGG decoding (in-memory safe)
scipy>=1.10.0              # High-quality resampling
av>=10.0.0                 # PyAV: direct FFmpeg bindings (fast, no subprocess)

# ML framework (optional - for future deep learning models)
# torch>=2.0.0
# transformers>=4.30.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0              # Async HTTP client for testing
websockets>=11.0           # WebSocket client for testing streaming

# HTTP client
requests>=2.28.0